name: Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-check:
    # Use the reusable build workflow for checks
    uses: ./.github/workflows/build-reusable.yml
    with:
      ref: ${{ github.ref }} # Pass the ref that triggered the workflow
      upload_artifact: false # No need to upload artifacts for CI checks